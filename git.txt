最简单的操作流程
bash
# 进入项目文件夹后执行这两条命令：
git add . && git commit -m "new"
git pull origin main && git push origin main




git add . && git commit -m "new" --allow-empty && git pull origin main --no-edit && git push origin main

